import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  // Main Palette
  static const Color navy = Color(0xFF1C252E);      // Deepest Navy
  static const Color steelBlue = Color(0xFF6D91AC); // Steel Blue
  static const Color iceBlue = Color(0xFFDCE6F2);   // Secondary Background
  static const Color arcticWhite = Color(0xFFFFFFFF); // Workspace

  // Semantic Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF10B981);
  static const Color greyText = Color(0xFF757575);
}